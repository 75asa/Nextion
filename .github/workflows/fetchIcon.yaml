name: fetchIcon

on:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  fetchIcon:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        yarn
        yarn build
        yarn start:fetchIcon
